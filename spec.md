# 車両走行記録アプリ 開発仕様書

## 1. 文書情報

* 仮称：マイカー走行日誌
* 開発方式：Claude Codeを使用
* 初期形態：スマートフォン・パソコン対応Webアプリ
* 開発段階：MVP（最小実用版）
* 作成日：2026年8月17日

---

# 2. アプリ概要

## 2.1 目的

毎朝ノートに手書きしていた「日付」「走行距離計の読み」を、スマホで記録できるようにする。あわせて、その日の運転の用途（仕事・プライベート・その他）と、給油した日・給油量を記録し、月ごとの用途別走行距離と燃費を自動で確認できるようにする。

## 2.2 アプリの特徴

* 毎日の走行距離計の読み（オドメーター）を記録する
* 記録すると、前回の記録からの走行距離（今日何km走ったか）を自動計算する
* 1日ごとに「仕事」「プライベート」「その他」のいずれかの用途を記録する
* 給油した日は、給油量（リットル）も記録する
* 給油と給油の間の走行距離 ÷ 給油量から、燃費（km/L）を自動計算する
* 毎月（1日〜末日）単位で、用途別の走行距離合計と、月間の平均燃費を確認できる
* 個人利用を前提とし、複数人・複数車両の管理は初期版には含めない

## 2.3 想定利用者

初期版は開発者本人が、日々のマイカー通勤・業務利用の記録のために使用する。

* 毎日手書きでオドメーターを記録している人
* 仕事とプライベートの走行を分けて記録しておきたい人（経費精算や確定申告の参考にしたい場合を含む）
* 燃費の推移を把握しておきたい人

---

# 3. 開発方針

## 3.1 初期版の考え方

初期版では、会員登録・複数車両管理・複数人共有は実装しない。まずは今までノートに書いていた内容をそのままアプリに置き換え、実際に使いながら必要な項目を見極める。

## 3.2 MVPに含める機能

* 走行記録（日付・走行距離計の読み・用途・給油の有無・給油量）の登録・編集・削除
* 前回記録からの走行距離の自動計算
* 給油記録に基づく燃費（km/L）の自動計算
* 月次（1日〜末日）の用途別走行距離集計
* 月次の給油量合計・平均燃費の表示
* 走行記録一覧（期間・用途での絞り込み）
* 燃費の推移確認（給油ごとの燃費履歴）
* CSV・JSON形式でのデータ書き出し
* データのバックアップと復元

## 3.3 初期版に含めない機能

* 複数車両の管理（車を切り替えて記録する機能）
* 複数人での共有・権限管理
* ガソリン代（金額）の記録（初期版は給油量のみ。将来検討）
* GPSによる自動走行距離計測
* 車検・点検・オイル交換などのメンテナンス記録（将来検討）
* 用途の自由なカテゴリ追加（初期版は「仕事」「プライベート」「その他」の3種固定）
* 外部会計ソフトとの連携
* 自動リマインド通知（毎日記録を促す通知など、将来検討）

---

# 4. 基本的な利用の流れ

1. アプリを開く
2. 「今日の記録を入力」から、今日の走行距離計の読みを入力する
3. 今日の用途（仕事／プライベート／その他）を選ぶ
4. 給油した日は「給油した」をONにし、給油量（リットル）を入力する
5. 保存すると、前回の記録からの走行距離が自動表示される
6. ホーム画面で、今月の走行距離合計・用途別内訳・直近の燃費を確認する
7. 月が変わったら、月次集計画面で先月分の確定した数値を確認する

---

# 5. 画面構成

## 5.1 画面一覧

| 画面ID | 画面名        | 主な役割                          |
| ---- | ---------- | ----------------------------- |
| S01  | ホーム画面      | 今日の入力状況、今月の走行距離・用途別内訳・直近の燃費を表示 |
| S02  | 走行記録入力画面   | 走行記録の新規登録・編集                  |
| S03  | 走行記録一覧画面   | 走行記録を一覧・絞り込み表示                |
| S04  | 走行記録詳細画面   | 1件の記録の詳細確認・編集・削除              |
| S05  | 月次集計画面     | 指定した月の用途別走行距離・給油量・平均燃費を表示     |
| S06  | 燃費履歴画面     | 給油ごとの燃費推移を一覧・グラフで表示           |
| S07  | 設定画面       | 表示設定、データ管理                     |
| S08  | データ管理画面    | 書き出し、バックアップ、復元                |
| S09  | ヘルプ・バージョン情報画面 | 簡単な使い方の説明とアプリのバージョン表示 |

---

## 5.2 S01 ホーム画面

### 表示内容

* 今日の日付
* 今日の記録が未入力の場合、「今日の記録を入力する」ボタンを目立たせて表示
* 今日の記録が入力済みの場合、その内容（走行距離計の読み・前回からの走行距離・用途）を表示
* 今月（1日〜今日）の走行距離合計
* 今月の用途別内訳（仕事／プライベート／その他、簡易表示）
* 今月の給油回数・給油量合計
* 直近の燃費（最新の給油区間の燃費）
* 今月の通勤割利用回数（朝・夕・合計）
* 「走行記録一覧を見る」「月次集計を見る」への導線

### 表示例

「今月はここまで320km走行しています（仕事210km・プライベート90km・その他20km）。直近の燃費は14.2km/Lです。」

---

## 5.3 S02 走行記録入力画面

### 入力項目

* 日付（必須、デフォルトは今日）
* 走行距離計の読み（必須、整数、単位km。例：51,649）
* 用途（必須、仕事／プライベート／その他。直前の入力を初期値として提案する）
* 給油した（トグル、初期値OFF）
  * ONにすると「給油量（リットル）」の入力欄が表示される（必須入力になる）
* 通勤割（チェックボックス「朝」「夕」の2つ、初期値OFF。高速道路の通勤割引利用を記録する。チェック1つ＝片道1回の利用としてカウントし、その日の記録に対して朝・夕それぞれON/OFFを保存する）
* メモ（自由記入、任意）

### 自動計算・表示

* 保存時に、直前の記録（日付が最も近い過去の記録）の走行距離計の読みとの差分を「今回の走行距離」として自動計算する
* 給油をONにした場合、直前の給油記録からの走行距離 ÷ 今回の給油量で、この区間の燃費（km/L）を自動計算する（直前に給油記録がない場合は「初回のため燃費は計算できません」と表示する）
* 入力中の画面にリアルタイムで「前回記録からの走行距離」を表示する

### 入力チェック

* 走行距離計の読みは、直前の記録の値以上でなければならない（オドメーターは通常減らないため。エラー例：「走行距離計の値が前回の記録より小さくなっています。入力内容を確認してください」）
* 給油をONにした場合、給油量は0より大きい数値でなければならない
* 同じ日付の記録が既に存在する場合は、上書き確認を表示する
* 日付は未来日を許可しない

---

## 5.4 S03 走行記録一覧画面

### 一覧表示内容

* 日付
* 走行距離計の読み
* 前回記録からの走行距離
* 用途（色分け表示）
* 給油マーク（給油した日はアイコン表示、給油量も表示）
* 通勤割マーク（朝・夕それぞれのON/OFF状況がひと目で分かるアイコン表示、および同じ日付に複数レコードがある場合はまとめたその日の通勤割利用回数（朝＋夕、0〜2回）を表示）

### 並び替え

* 日付順（新しい順・古い順）

### 絞り込み

* 期間（今月／先月／任意の期間を指定）
* 用途（仕事／プライベート／その他／すべて）
* 給油した日のみ

---

## 5.5 S04 走行記録詳細画面

### 表示内容

* 記録の全項目
* 前回記録からの走行距離
* （給油記録の場合）この区間の燃費

### 操作

* 編集
* 削除（確認ダイアログあり）

削除確認文例：

「この記録を削除します。前後の記録の走行距離・燃費の計算に影響する場合があります。元に戻せません。」

削除すると、その記録を挟んでいた前後の記録同士で走行距離・燃費が再計算される。

---

## 5.6 S05 月次集計画面

### 表示内容

* 月選択（前月・今月・任意の月を「◯年◯月」の形式で選択）
* 選択した月の走行距離合計
* 用途別内訳（仕事／プライベート／その他、金額ではなく距離での内訳。グラフまたはバー表示）
* 選択した月の給油回数・給油量合計
* 選択した月における平均燃費（月内に発生した給油区間の燃費の平均）
* 選択した月の通勤割利用回数（朝の回数・夕の回数・合計回数）
* CSV書き出しボタン（この月の記録のみを対象に出力）

### 集計の考え方

* 「月の走行距離合計」は、その月の1日〜末日の間に記録された走行距離（前回記録からの差分）の合計とする
* 月をまたぐ給油区間（例：先月末に給油し、今月に入ってから次の給油をした場合）の燃費は、給油した日（今月側）の月に計上する

---

## 5.7 S06 燃費履歴画面

### 表示内容

* 給油記録ごとの一覧（給油日・給油量・区間走行距離・燃費）
* 燃費の推移グラフ（折れ線グラフ、横軸：給油日、縦軸：燃費km/L）
* 期間内の平均燃費・最高燃費・最低燃費

---

## 5.8 S07 設定画面

### 設定項目

* 表示名
* 距離の単位（初期はkm固定、表示のみ）
* 用途の初期値提案の有無（直前の用途を提案するかどうか）
* テーマ設定
* データ保存方式

---

## 5.9 S08 データ管理画面

### 機能

* CSV書き出し（全期間、または月を指定して出力）
* JSONバックアップ（全データを一括保存）
* JSONから復元
* 全データ削除
* バックアップ日時の表示

全データ削除時は、確認文を入力させる。

---

## 5.10 S09 ヘルプ・バージョン情報画面

初めて使う時でも迷わないよう、簡単な使い方とアプリのバージョン情報をまとめて表示する画面。設定画面から導線を設ける。

### 表示内容

**使い方セクション**（見出しごとに区切って表示、テキストのみで簡潔に）

1. **毎日の記録**：走行前または走行後に、車の走行距離計（オドメーター）に表示されている数字をそのまま入力してください。
2. **用途の選び方**：その日の運転が主に「仕事」「プライベート」「その他」のどれだったかを選んでください。
3. **給油した日**：給油した日は「給油した」をONにして、給油量（リットル）を入力してください。次回給油時に自動で燃費が計算されます。
4. **走行距離の自動計算について**：前回記録した日からの走行距離が自動で計算されます。記録を忘れた日があっても、次に記録した時点で正しく計算されます。
5. **月次集計について**：ホーム画面や月次集計画面で、今月の用途別走行距離や平均燃費を確認できます。
6. **データを守るために**：設定画面の「データ管理」から、定期的にJSONバックアップを取っておくことをおすすめします。

**バージョン情報セクション**

* アプリ名：マイカー走行日誌
* バージョン番号（例：v1.0.0、更新のたびに更新する）
* 最終更新日
* 開発：Claude Codeで作成

**更新履歴セクション**

* バージョンごとの更新内容を、新しいものから順に一覧表示する
* 各項目に「バージョン番号」「更新日」「更新内容の概要」を表示する

### 操作

* 設定画面から「ヘルプ」を開く導線を設ける
* ホーム画面の初回起動時（データが1件もない状態）にも、ヘルプ画面への案内を軽く表示する

---

# 6. 機能一覧

| 機能ID | 機能名        | 内容                              | 優先度 |
| ---- | ---------- | ------------------------------- | --- |
| F01  | 走行記録登録     | 日付・走行距離計・用途・給油情報を登録する           | 必須  |
| F02  | 走行記録編集・削除  | 記録の内容を修正・削除する                    | 必須  |
| F03  | 走行距離自動計算   | 前回記録との差分から、今回の走行距離を計算する          | 必須  |
| F04  | 燃費自動計算     | 給油区間の走行距離 ÷ 給油量から燃費を計算する         | 必須  |
| F05  | 用途別集計      | 月単位で、用途ごとの走行距離を集計する              | 必須  |
| F06  | 月次燃費集計     | 月単位で、給油量合計・平均燃費を集計する             | 必須  |
| F07  | ホームサマリー    | 今日の入力状況、今月の走行距離・燃費の概要を表示する       | 必須  |
| F08  | 走行記録一覧・絞込  | 期間・用途・給油有無で絞り込む                  | 必須  |
| F09  | 燃費履歴・推移グラフ | 給油ごとの燃費をグラフで表示する                 | 推奨  |
| F10  | 用途初期値提案    | 直前に入力した用途を初期値として提案する             | 推奨  |
| F11  | 削除時の再計算    | 記録削除後、前後の記録の走行距離・燃費を再計算する        | 必須  |
| F12  | CSV出力      | 走行記録データを期間指定で出力する                | 必須  |
| F13  | JSONバックアップ | 全データをJSONで保存する                  | 必須  |
| F14  | JSON復元     | バックアップから復元する                     | 必須  |
| F15  | PWA化       | ホーム画面への追加に対応する                   | 推奨  |
| F16  | 複数車両対応     | 車両を切り替えて記録できるようにする               | 将来  |
| F17  | ガソリン代記録    | 給油時の金額も記録し、燃料費も集計する              | 将来  |
| F18  | メンテナンス記録   | 車検・点検・オイル交換等の記録を追加する             | 将来  |
| F19  | ヘルプ表示      | 簡単な使い方の説明を画面内で確認できる              | 必須  |
| F20  | バージョン情報表示  | アプリのバージョン番号・最終更新日を表示する          | 必須  |
| F21  | 通勤割チェック    | 走行記録に「朝」「夕」の通勤割（高速道路の通勤割引）利用チェックを記録する | 必須  |
| F22  | 通勤割集計      | 日ごとの通勤割利用回数を一覧に表示し、月ごとの利用回数（朝・夕・合計）を集計する | 必須  |

---

# 7. データ項目

## 7.1 走行記録テーブル

テーブル名：`daily_logs`

| 項目名              | データ型 | 必須 | 内容                                     |
| ---------------- | ---- | -- | -------------------------------------- |
| id                | 文字列  | 必須 | 記録固有ID                                  |
| log_date          | 日付   | 必須 | 記録日                                     |
| odometer_reading  | 数値   | 必須 | 走行距離計の読み（累計km、整数）                       |
| distance_from_prev | 数値  | 任意 | 直前の記録からの走行距離（自動計算、保存時に確定値として記録。初回はnull） |
| purpose           | 文字列  | 必須 | 仕事／プライベート／その他                          |
| is_refueled       | 真偽値  | 必須 | この日に給油したかどうか                           |
| fuel_liters       | 数値   | 任意 | 給油量（リットル、is_refueled=trueの場合は必須）        |
| fuel_efficiency   | 数値   | 任意 | この給油区間の燃費（km/L、自動計算。給油日のみ値が入る）          |
| commute_discount_morning | 真偽値 | 必須 | 通勤割（朝）を利用したかどうか（高速道路の通勤割引、片道1回分）        |
| commute_discount_evening | 真偽値 | 必須 | 通勤割（夕）を利用したかどうか（高速道路の通勤割引、片道1回分）        |
| memo              | 文字列  | 任意 | メモ                                      |
| created_at        | 日時   | 必須 | 登録日時                                    |
| updated_at        | 日時   | 必須 | 更新日時                                    |

## 7.2 設定テーブル

テーブル名：`app_settings`

| 項目名                | データ型 | 内容                  |
| ------------------ | ---- | ------------------- |
| display_name       | 文字列  | 表示名                 |
| suggest_last_purpose | 真偽値 | 直前の用途を初期値として提案するか   |
| theme               | 文字列  | 画面テーマ                |
| storage_mode        | 文字列  | 保存方式                |
| updated_at          | 日時   | 設定更新日時               |
| app_version         | 文字列  | アプリのバージョン番号（コード内に定数として保持し、ヘルプ画面に表示） |

---

# 8. 入力チェック

## 8.1 走行記録

* 走行距離計の読み：必須、整数、直前の記録の値以上であること（例外的に減った場合はエラーとし、修正を促す。将来的にメーター交換等の例外処理を検討）
* 日付：必須、未来日は入力不可、過去日は入力可能
* 用途：必須（仕事／プライベート／その他のいずれか）
* 給油量：`is_refueled` がtrueの場合のみ必須、0より大きい数値
* 同一日付の記録が既に存在する場合、上書き確認を表示する

---

# 9. 走行距離・燃費の計算仕様

## 9.1 走行距離の計算

`今回の走行距離 = 今回の odometer_reading − 直前の記録（日付が最も近い過去の記録）の odometer_reading`

直前の記録が存在しない場合（最初の1件目）は、走行距離は計算せず「初回記録」として扱う。

日付が連続していなくても（記録を忘れた日があっても）、直前に登録されている記録を基準に計算する。そのため、「前日の走行距離」ではなく「前回記録からの走行距離」という表現を画面上では使用する。

## 9.2 燃費の計算

`この給油区間の燃費（km/L） = 今回の給油日の odometer_reading − 直前の給油日の odometer_reading） ÷ 今回の給油量（L）`

* 直前に給油記録が存在しない場合は、燃費を計算せず「初回の給油のため燃費は計算できません」と表示する
* 給油しない日の走行記録には、燃費は記録されない（燃費は給油日にのみ紐づく）
* 満タン法（毎回満タンまで給油する前提）を採用しており、満タンにしなかった場合は数値の精度が下がる旨を、給油記録入力画面に注記として表示する

## 9.3 用途別集計（月次）

* 対象月（1日〜末日）に属する走行記録の `distance_from_prev` を、`purpose` ごとに合計する
* 月の1件目の記録が、前月末の記録からの差分である場合も、その走行距離は「今月分」として計上する（走行が発生した記録日基準で月に振り分ける）

## 9.4 月次の燃費集計

* 対象月内に発生した給油記録（`fuel_efficiency` が算出されているもの）の平均値を「月間平均燃費」とする
* 対象月内に給油記録が1件もない場合は「この月は給油記録がありません」と表示する

## 9.4a 通勤割の集計

* 日ごとのカウント：同じ日付に複数レコードがある場合はまとめて、`commute_discount_morning`・`commute_discount_evening` のON数の合計（0〜2回）をその日の通勤割利用回数とする
* 月間カウント：対象月（1日〜末日）に属する走行記録について、`commute_discount_morning` がONの件数（朝の回数）、`commute_discount_evening` がONの件数（夕の回数）、およびその合計（合計回数）を集計する

## 9.5 記録削除時の再計算

ある記録を削除すると、その前後の記録が直接つながることになるため、以下を再計算する。

* 削除した記録の次の記録の `distance_from_prev` を、削除した記録のさらに前の記録との差分で再計算する
* 削除した記録が給油記録だった場合、次の給油記録の `fuel_efficiency` を、削除した給油記録のさらに前の給油記録を基準に再計算する

---

# 10. UI・操作性

## 10.1 基本方針

* スマートフォンを縦向きで使いやすくする
* 毎日の入力を最短の手数で行えるようにする（走行距離計の読みと用途の2つだけで登録できるのが基本形）
* 用途は色分け表示する（例：仕事＝青、プライベート＝緑、その他＝グレー）
* 走行距離・燃費の数値は見やすく大きめに表示する

## 10.2 推奨文字サイズ

* 通常本文：16px以上
* 走行距離・燃費の数値：20px以上、太字
* 見出し：18px以上

## 10.3 ボタン・操作

* 高さ44px以上
* 「削除」操作には必ず確認を入れる
* 走行距離計の読みは数字専用キーボードで入力できるようにする

## 10.4 下部固定タブメニュー

全画面共通で、画面下部に固定表示するタブメニューを設ける。

### タブ項目（4つ）

| ラベル  | アイコン | 遷移先   |
| ---- | ---- | ----- |
| ホーム  | 🏠   | S01（ホーム画面） |
| 記録一覧 | 📝   | S03（走行記録一覧画面） |
| 月次集計 | 📊   | S05（月次集計画面） |
| ヘルプ  | ⚙️   | S09（ヘルプ・バージョン情報画面） |

### 表示・操作仕様

* 画面下部に固定表示し、スクロールしても常に見える状態を維持する
* 現在表示中の画面に対応するタブを色付き（アクセントカラー）でハイライトする
* 各タブをタップすると、対応する画面へ遷移する
* スマートフォンでの表示を優先し、タップしやすいサイズ（アイコン＋短いラベル）で配置する
* タブメニューに隠れないよう、各画面のコンテンツ下部に余白を確保する

---

# 11. データ保存とプライバシー

## 11.1 初期版

初期版は、ブラウザ内のローカルデータベース（IndexedDB）に保存する。外部サーバーへ自動送信しない。

## 11.2 注意点

毎日の記録が積み重なるデータのため、JSONバックアップ機能を早い段階で用意し、定期的にバックアップを取ることを推奨する。

## 11.3 将来版

複数端末での利用や、複数車両の管理を行う場合は、クラウド保存（Supabase等）を検討する。

---

# 12. 推奨技術構成

* フロントエンド：Next.js
* 開発言語：TypeScript
* デザイン：Tailwind CSS
* データ保存：IndexedDB
* IndexedDB操作：Dexie.js
* 入力検証：Zod
* 日付処理：date-fns
* グラフ（燃費推移）：Recharts
* アプリ形式：PWA対応
* ソース管理：GitHub

---

# 13. 開発ステップ

## 第1段階：基本の走行記録管理

* プロジェクト作成
* 走行記録の登録・編集・削除
* 走行距離の自動計算（前回記録との差分）
* 走行記録一覧画面（基本表示）
* ホーム画面（簡易版）

## 第2段階：給油・燃費機能

* 給油記録の登録（走行記録入力画面への統合）
* 燃費の自動計算
* 燃費履歴画面（一覧・グラフ）
* 記録削除時の再計算ロジック

## 第3段階：月次集計

* 月次集計画面（用途別走行距離、給油量合計、平均燃費）
* ホーム画面での今月サマリー強化

## 第4段階：使いやすさ強化

* 用途の初期値提案
* 走行記録一覧の絞り込み強化
* 入力チェック（オドメーターの逆行防止等）の強化

## 第5段階：データ管理・PWA化

* CSV出力（月指定）
* JSONバックアップ・復元
* PWA化（ホーム画面への追加対応）
* ヘルプ・バージョン情報画面の追加

## 第6段階：将来拡張

* 複数車両対応
* ガソリン代（金額）の記録
* メンテナンス記録
* 通知・リマインド機能

---

# 14. 完成条件

初期版は、次の条件を満たした時点で完成とする。

1. スマートフォンとパソコンで画面が崩れない
2. 走行記録（日付・走行距離計・用途・給油情報）を登録・編集・削除できる
3. 前回記録からの走行距離が自動計算される
4. 給油区間の燃費が自動計算される
5. 記録を削除しても、前後の記録の走行距離・燃費が正しく再計算される
6. 月次集計画面で、用途別走行距離・給油量合計・平均燃費が確認できる
7. ホーム画面で今月のサマリーが分かる
8. 走行記録一覧を期間・用途・給油有無で絞り込める
9. 燃費履歴をグラフで確認できる
10. CSV形式で記録を書き出せる
11. JSON形式でバックアップと復元ができる
12. ページを閉じてもデータが残る
13. 削除の前に確認が表示される
14. スマートフォンのホーム画面に追加できる（PWA）

---

# 15. 開発上の重要事項

* 毎日の入力のしやすさを最優先する（走行距離計の読みと用途の入力だけで完結する設計にする）
* オドメーターは通常減らないため、入力チェックを確実に行う
* 燃費の計算ロジック（満タン法）は間違えやすい部分なので、給油記録の削除・編集時の再計算を含めてテストする
* 用途別集計・燃費集計は、これまでノートで手計算していた内容の代替となるため、正確性を最優先する
* バックアップ機能を早い段階で実装する

---

# 16. Claude Codeへの初回開発指示

以下の仕様で、スマートフォンとパソコンに対応したPWA形式のWebアプリを作成してください。

アプリ名は「マイカー走行日誌」です。Next.js、TypeScript、Tailwind CSSを使用してください。データはIndexedDBへ保存し、Dexie.jsで操作してください。

最初の開発範囲は次のとおりです（第1段階：基本の走行記録管理）。

1. ホーム画面（簡易版：今日の入力状況表示）
2. 走行記録一覧画面
3. 走行記録入力・編集画面（日付・走行距離計の読み・用途・給油トグル・給油量・メモ）
4. 走行記録詳細画面

走行記録の入力項目は、日付（必須）、走行距離計の読み（必須、整数km）、用途（必須、仕事／プライベート／その他）、給油した（トグル、ONの場合は給油量が必須）、メモです。

保存時に、直前の記録（日付が最も近い過去の記録）の走行距離計の読みとの差分を「前回記録からの走行距離」として自動計算し、リアルタイムで表示してください。走行距離計の読みが直前の記録より小さい場合はエラーとし、登録できないようにしてください。

燃費計算・月次集計・燃費履歴グラフの機能は、第1段階の完成後、別途追加で依頼します。まずは走行記録の基本的なCRUD（登録・編集・削除・一覧表示）と、走行距離の自動計算を確実に動く形で実装してください。

コードは機能ごとに分割し、データ型、入力検証、エラー処理を実装してください。最初に全体のフォルダ構成と作業手順を提示し、その後、段階的に実装してください。

---

# 17. 改訂履歴

| バージョン | 更新日        | 更新内容                                       |
| ----- | ---------- | ------------------------------------------ |
| v1.7.0 | 2026-09-23 | 画面下部に固定タブメニュー（ホーム・記録一覧・月次集計・ヘルプ）を追加（10.4） |
| v1.6.0 | 2026-09-03 | 通勤割（朝・夕）チェック機能と月間サマリーへの回数表示を追加             |
